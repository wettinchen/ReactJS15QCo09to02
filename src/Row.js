import React from 'react'

const Row = ({ item }) => {
  return (
    <tr>
        {Object.entries(item).map(([key, value]) => {
            return (

            )
        })}
    </tr>
  )
}

export default Row